! Must have
? Must have?
* Must not have
$ Code mistakes
~ Optional

----

! messages (error, success)
$ generate map outside from api wirh special argument
$ Define rules for each class type (Model - No persistence, DAO - Not to much logic, API encapsulate different logics from daos, ...)
? Delete languages
